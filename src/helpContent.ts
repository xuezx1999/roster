// ROSTER 使用说明（Markdown 原文）。
// 由 helpMarkdown.ts 解析为树结构后在 HelpPage 渲染；
// 标题层级决定折叠/展开的范围，新增/修改内容只需编辑此处。

export const HELP_CONTENT = `# ROSTER 使用说明

## 当前版本

- 当前版本号：0.9.12。
- 版本更新日期：2026-08-13。
- 主要更新内容：取消中英文之间自动加窄空格功能：任务内容、列表标题与使用说明恢复为原文显示，中英文之间如需留白请手动输入半角空格（原窄空格仅为显示层效果，不影响存储数据）。

## 数据存储

- ROSTER 为**纯本地应用**，其数据完全存储在您的本地 IndexedDB 中，没有云同步功能。
- 尽管您可以在桌面端浏览器、移动端浏览器使用 ROSTER，但还是强烈建议您以**移动端 PWA** 的形式使用，即「添加到主屏幕」。
- 不同使用环境的数据存储情况是。

### 桌面端浏览器

- 通过网址访问 ROSTER，数据落在该浏览器当前配置文件的 IndexedDB，按站点 Origin 隔离。
- 重新打开、重启浏览器、关机都持久保留；只有两种情况会丢：① 你手动「清除网站数据/站点数据」；② 系统磁盘空间极度紧张触发浏览器回收（罕见）。
- 同一台电脑上不同浏览器、或同一浏览器的不同 Profile，数据互不相通，各自独立一份。

### 移动端浏览器

- 通过网址访问 ROSTER，数据同样存在该手机浏览器的 IndexedDB，按站点 Origin 隔离，刷新/重开页面都在。
- **⚠️ iOS Safari 特有风险**：WebKit 对「7 天内未交互的网站」会清除其脚本可写存储。所以手机浏览器里偶尔打开一次、长期不用的 ROSTER，数据可能被系统自动清掉。安装成 PWA（见下）可避开这条限制。
- 其他浏览器一般更持久，但仍受存储空间回收与「清除浏览数据」影响。
- 隐私模式/无痕模式下部分浏览器会禁用或限制 IndexedDB，应用会走 \`initError\` 提示，数据可能不落盘。

### 移动端 PWA

- 应用外壳经 Service Worker 预缓存，首次加载后即可离线打开使用，数据读写仍是本地 IndexedDB。
- iOS：安装的 PWA 有独立的存储容器，与 Safari 浏览器内的网站数据完全分开——即便同一个域名，在 Safari 里录的数据和在主屏 App 里录的数据互不共享；同时它不受上面那条 7 天清除限制，只要 App 还在主屏就持久保留。
- Android：PWA 数据持久性很好；卸载该 PWA（从主屏删除）会清空其 IndexedDB。

### PWA 添加方式

#### iOS

1. 用 Safari 访问 ROSTER；
2. 点底部地址栏右侧的「分享」按钮；
3. 点击「分享」，在弹出菜单中下滑并找到「添加到主屏幕」；
4. 主屏出现图标，之后从主屏打开就是独立 App。

#### Android

1. 用浏览器访问 ROSTER；
2. 浏览器会自动弹出安装横幅，或在菜单栏中找到「安装应用」「添加到主屏幕」等选项；
3. 确认后主屏生成图标，从主屏打开即独立 App。

#### 桌面端浏览器（安装应用）

- 桌面端也可以把 ROSTER 安装为应用使用。
- Chromium 内核浏览器打开 ROSTER 后，通常会在地址栏右侧提示「安装 ROSTER 应用」，根据提示点击安装即可；您也可以在菜单栏中找到「应用」，点击「将此站点安装为应用」进行安装，有时「应用」会藏在「更多工具」选项卡中。
- Safari 浏览器点击「分享」按钮后，可以「添加至程序坞」。

---

## 数据备份

- 不同环境之间 ROSTER 的数据不会自动同步，要**换设备/换浏览器/做长期备份**，可以使用应用内的导出/导入功能，把数据以文件形式搬过去。
- 此外，**卸载 PWA 会清空数据**，在卸载前请备份；重要数据也请记得定期备份。

### 导出数据

- 点击菜单中的「导出数据」，将立即下载一个 JSON 文件，命名为 \`ROSTER-YYYYMMDD-HHMMSS\`，内容为导出那一刻的全部数据。
- 文件为标准 RosterExport 格式，含所有清单、任务及当前选中的列表。
- 务必把它存到安全处（云盘、发邮件给自己、本地文件夹等）。这个文件才是换设备/恢复时真正能用的备份。

### 导入数据

- 点击菜单中的「导入数据」，选取之前导出的 JSON 文件，点击确认后导入完成。
- **⚠️ 导入是「整库覆盖」，不是合并**：当前设备/浏览器里的所有数据会被导入文件中的内容完整替换。只想补一部分时，请先导出当前数据留底。
- 导入非 ROSTER 格式或结构损坏会被拒绝并提示，不会破坏现有数据。

### 自动导出（仅桌面端）

- 桌面浏览器（鼠标操作、非触屏）下，距上次导出 >7 天且清单非空时，启动会自动下载一份 JSON 备份，并短暂显示「已自动导出备份」提示。
- 移动端不自动下载，避免打扰。
- 这是「省心」兜底，不能替代你手动导出并把文件存到云盘等安全位置——自动文件默认在本机「下载」目录，清了就没了。

---

## 基础功能

### 修改列表标题

- 单击列表顶部的**标题文字**即进入编辑状态，直接输入修改；按 **\`Enter\`** 或点击空白处保存，**\`Esc\`** 取消。

### 添加待办事项

- 点击列表底部的 **\`+ ADD\`** 按钮，唤出输入框；
- 或在列表**空白处双击**（桌面端在某一列区域内双击即可，移动端在单列空白处双击）；
- 输入内容后按 **\`Enter\`** 自动提交，**\`Esc\`** 取消；输入框失焦也会提交（前提是已输入内容）。

### 排序与删除单条事项

- **移动端**：长按某条事项约 0.45 秒；**桌面端**：在该条上**右键**，进入「操作模式」。
- 进入后事项右侧出现 **\`≡\`** 拖拽手柄，按住拖动即可调整顺序；同时出现 **\`删除此条\`**，点击删除该条记录。
- 完成排序（拖放结束）或删除后自动退出「操作模式」。

### 菜单（右上角）

- 点击右上角的 **\`≡\`** 菜单按钮，展开更多功能：新增列表、清除完成、导出/导入、切换主题、音效开关、删除列表等（详见各节）。

### 多列表与切换

- 在菜单中点击 **\`+ 新增列表\`** 新建清单；
- **多列表时**：移动端在屏幕上**左右滑动**切换列表，桌面端按键盘 **\`←\` / \`→\`** 方向键切换视图（首尾不回绕）；
- 桌面宽屏（≥768px）会同时显示多列，每列可独立操作。

### 清除完成

- 菜单中的 **「清除完成」** 仅对**当前正在查看的列表**生效，不会影响其它列表的已完成项。

### 标记完成与自动沉底

- 单击待办事项左侧括号按钮标记「进行中」（显示空心圆圈 \`○\`，含义可按您的习惯自定义），双击左侧括号按钮标记「已完成」（显示实心圆圈 \`●\` 并置灰）；
- 进行中的条目**自动置顶**，已完成的条目**自动沉底**，与普通待办分开；
- 提示：单击事项**文字本身**是进入**编辑**状态，不是标记完成。

### 主题切换

- 菜单中的 **\`◐\`** 项可在 **亮色 / 暗色** 之间切换，选择会记住，下次打开仍是该主题。

### 音效开关

- 菜单中的 **\`♪\`** 项可开启 / 关闭交互音效，默认开启，选择会记住，下次打开仍是该设置；
- 开启时，菜单、主题与音效切换、标记完成、删除、导出等操作会播放轻量提示音；关闭后界面操作不受影响，只是不再发声。

---

## 开源仓库

- 本项目开源仓库在：https://github.com/xuezx1999/roster。
- 本项目的设计理念、技术架构受到开源项目 Brew Guide 的启发，项目地址是：https://chu3.top/brewguide。
- 本项目可以通过 https://roster.xuezhixiang.fun/ 在线使用，但也推荐您 Fork 开源仓库自行调整、部署使用，以防服务不稳定。
- 您也可以在我的个人网站 https://xuezhixiang.fun/ 上看到我的更多作品。
`
